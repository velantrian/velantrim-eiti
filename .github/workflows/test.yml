name: Tests

on:
  push:
    branches: [main, "claude/**"]
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  bounded-remediate:
    name: Bounded admission remediation delivery
    if: github.event_name == 'pull_request' && github.head_ref == 'fix/eiti-admission-hardening' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/eiti-admission-hardening
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Apply bounded patch
        run: python .github/bounded_remediate.py
      - name: Run JS regression suite
        run: node --test velantrim_core/tests_js/*.test.js
      - name: Run Python regression suite
        run: |
          python -m pip install --upgrade pip
          pip install -r velantrim_core/requirements.txt
          (cd velantrim_core && pytest tests/ -v)
      - name: Remove delivery scaffolding and push bounded commit
        run: |
          git fetch origin main
          git show origin/main:.github/workflows/test.yml > .github/workflows/test.yml
          rm -f .github/workflows/_bounded-remediation.yml .github/bounded_remediate.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html velantrim_core/tests_js/apply_analysis.test.js velantrim_core/tests_js/epistemic_admission.test.js .github/workflows/test.yml .github/workflows/_bounded-remediation.yml .github/bounded_remediate.py
          git commit -m 'fix(architecture): separate learning proposals from epistemic admission'
          git push origin HEAD:fix/eiti-admission-hardening

  js-unit:
    name: JS unit tests (index.html pure logic)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Run Node test runner
        run: node --test velantrim_core/tests_js/*.test.js

  python-core:
    name: Python KB tooling tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        working-directory: velantrim_core
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Run pytest
        working-directory: velantrim_core
        run: pytest tests/ -v

  e2e-smoke:
    name: Playwright smoke tests (PWA)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install Playwright + browser
        working-directory: velantrim_core/e2e
        run: |
          npm ci || npm install
          npx playwright install --with-deps chromium
      - name: Run smoke tests
        working-directory: velantrim_core/e2e
        run: npx playwright test --reporter=line
